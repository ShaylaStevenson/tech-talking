<!-- When the user selects a single post from the homepage-->
<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Created by {{user.user_name}} on {{format_date date_created}}</p>
  
  <p>
    <button class="btn btn-primary">Leave a Comment</button>
  </p>
</div>